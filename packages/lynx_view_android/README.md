# lynx_view_android

Android implementation of [`lynx_view`](https://pub.dev/packages/lynx_view). Depend on `lynx_view` directly — this package has no standalone Dart API and is pulled in automatically as the Android platform implementation.
